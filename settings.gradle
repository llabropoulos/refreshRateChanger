dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral() // JCenter has been removed
    }

}
rootProject.name = "refreshRateChanger"
include ':app'
